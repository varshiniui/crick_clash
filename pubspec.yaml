name: cricket_quiz_gift
description: A cricket quiz gift app 🏏
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1 
  string_similarity: ^2.0.0
  shared_preferences: ^2.2.2
  animate_do: ^3.3.4
  

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  assets:
    - lib/data/players.json
    - images/players/
    
